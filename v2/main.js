/* ==========================================================================
   Autokatalyst — behaviour
   Vanilla JS. One init function per component. Scoped queries.
   data-* attributes for wiring, data-state for state.

   The case study section is a scroll composition rather than a control, so
   it carries no behaviour of its own. Its motion lives in
   motion/motion-cases.js.
   ========================================================================== */
